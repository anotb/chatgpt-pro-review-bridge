[project]
name = "chatgpt-pro-review-bridge"
version = "0.7.4"
description = "Python client for visible ChatGPT Pro questions and code reviews through the shared local backend."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [
  { name = "Adam Allcock" }
]
keywords = ["codex", "chatgpt", "browser-automation", "visible-ui", "agents", "sdk"]
classifiers = [
  "Development Status :: 5 - Production/Stable",
  "Intended Audience :: Developers",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Typing :: Typed"
]
dependencies = [
  "pydantic>=2,<3"
]

[project.scripts]
chatgpt-thread = "codex_chatgpt_control.thread_entrypoint:main"

[project.urls]
Homepage = "https://github.com/anotb/chatgpt-pro-review-bridge"
Issues = "https://github.com/anotb/chatgpt-pro-review-bridge/issues"
Source = "https://github.com/anotb/chatgpt-pro-review-bridge"

[project.optional-dependencies]
dev = [
  "pyright>=1.1.390"
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
codex_chatgpt_control = ["py.typed"]
